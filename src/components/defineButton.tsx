import { defineComponent, defineProps, h } from 'vue'
import { NButton } from 'naive-ui'

const component = {
  name: 'DefineButton',
  props: ['title'],
  setup() {
    return h('')
  },
}

export default component
